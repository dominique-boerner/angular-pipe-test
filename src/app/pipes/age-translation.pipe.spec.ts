import { AgeTranslationPipe } from './age-translation.pipe';

describe('AgeTranslationPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeTranslationPipe();
    expect(pipe).toBeTruthy();
  });
});
